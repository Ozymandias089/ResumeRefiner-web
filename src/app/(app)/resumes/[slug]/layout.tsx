import "@/styles/print-resume.css";

export default function ResumeSlugLayout({
                                             children,
                                         }: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
